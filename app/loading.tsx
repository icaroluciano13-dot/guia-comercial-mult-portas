export default function Loading() {
  return (
    <main className="auth-shell">
      <section className="auth-card auth-loading" aria-live="polite" aria-busy="true">
        <div className="brand-mark auth-mark">MP</div>
        <strong>Preparando seu espaço…</strong>
        <span>Carregando o Guia Comercial Mult Portas</span>
      </section>
    </main>
  );
}
